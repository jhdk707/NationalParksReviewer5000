<section class="hero is-fullheight is-centered">
  <div class="columns is-multiline is-centered reviews-container">
    {{#each posts as |post|}}
    <div class="column is-one-third">
      <div class="box">
        {{> post-info post}}
      </div>
    </div>
    {{/each}}
  </div>
</section>
